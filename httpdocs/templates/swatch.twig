<div class="swatch {{ swatchSize }}" data-hex="{{ color.hex }}">
    <div class="color-box"
         style="background-color: {{ color.hex }}">
    </div>
    <div class="color-label">
        <div class="color-hex">{{ color.hex }}</div>
    </div>
</div>
